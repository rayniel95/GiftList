// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
    basePath: '/GiftList',
    assetPrefix:'/GiftList/',
    experimental: {
        appDir: true,
    },
}

module.exports = nextConfig